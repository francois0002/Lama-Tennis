import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lamatennis.app',
  appName: 'lama-tennis',
  webDir: 'dist/lama-tennis/browser'
};

export default config;
